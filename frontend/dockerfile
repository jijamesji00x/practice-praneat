# Use an official web server image as the base image
FROM nginx:latest

# Copy the HTML file to the default document root directory of Nginx
COPY index.html /usr/share/nginx/html

# Expose port 80 (default port for HTTP traffic)
EXPOSE 80

# Start the Nginx server when the container is run
CMD ["nginx", "-g", "daemon off;"]
